#
#  For working with the dotfiles repository
#
if [ -d ~/.dotfiles ]; then
   alias DOT='git --work-tree=$HOME --git-dir=$HOME/.dotfiles/'
fi


#
# Move up
#
alias ..='cd ..'

#alias yay='proxychains -q yay'
alias ejectusb='udisksctl unmount -b /dev/sda1; udisksctl power-off -b /dev/sda'
